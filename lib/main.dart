import 'package:flutter/material.dart';
import 'package:prova2/listview_eletro.dart';

void main() => runApp(
      MaterialApp(
        title: 'Carregando dados',
        home: ListViewEletro(),
      ),
    );
